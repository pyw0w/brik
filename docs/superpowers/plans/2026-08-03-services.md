# Сервисы (defineService + ServiceMap) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить в Brik первый-классный механизм сервисов — глобальных, жизненно-управляемых зависимостей (например, HTTP-клиентов к сторонним API), которые модуль декларирует и получает типизированно в `ctx.services`.

**Architecture:** Сервисы живут в `src/services/<name>/service.ts` (авто-дискавери как у модулей), объявляются через `defineService` и расширяют интерфейс `ServiceMap` (аугментация типов). Модуль декларирует нужные сервисы (`services: ['weather']`), ядро строит только нужные (init до setup модулей), инжектит в `setup`/`onReady`/`run` как `ctx.services: ServiceMap`, и закрывает при shutdown в обратном порядке.

**Tech Stack:** TypeScript, bun, zod, discord.js (не трогаем).

## Global Constraints

- Всё через **bun**: `bun test`, `bun run typecheck`, `bun run check:boundaries`, `bun run docs:build`.
- Модули и сервисы импортируют **только** `../../core/index.ts` (код) и `../../core/testing.ts` (тесты). `bun run check:boundaries` это проверяет.
- Тесты co-located рядом с кодом; покрытие `test:coverage` ≥ 0.7.
- Комментарии и сообщения об ошибках — русские (как в кодовой базе), идентификаторы — английские.
- Типизация сервисов — через аугментацию `ServiceMap` (fallback из спеки `docs/superpowers/specs/2026-08-03-services-design.md`).
- Коммиты частые, по одному на завершённый шаг.

---

### Task 1: Контракт — `defineService` + `ServiceMap`

**Files:**
- Create: `src/core/service.ts`
- Modify: `src/core/index.ts`
- Test: `src/core/service.test.ts`

**Interfaces:**
- Produces: `defineService(def)` → `Service`; `ServiceMap`; `ServiceInitContext<O>`; `ServiceDef<O>`; `Service<O>`.
- Потребители: Task 2 (module/handler ctx), Task 3 (ServiceRegistry), Task 8 (demo).

- [ ] **Step 1: Написать падающий тест `src/core/service.test.ts`**

```ts
import { describe, expect, test } from 'bun:test';
import { defineService, type ServiceMap } from '../index.ts';

describe('defineService', () => {
  test('сохраняет name/description/init и возвращает api из init', () => {
    const svc = defineService<{ url: string }>({
      name: 'echo',
      description: 'Тестовый сервис',
      optionsSchema: undefined,
      init: ({ options }) => ({ url: options.url }),
      close: () => {},
    });
    expect(svc.name).toBe('echo');
    expect(svc.description).toBe('Тестовый сервис');
    const api = svc.init({ options: { url: 'https://x' }, logger: {} as never, memory: {} as never });
    expect(api).toEqual({ url: 'https://x' });
  });

  test('close опционален', () => {
    const svc = defineService({ name: 'noop', init: () => 42 });
    expect(svc.close).toBeUndefined();
    expect(svc.init({ options: undefined, logger: {} as never, memory: {} as never })).toBe(42);
  });

  test('ServiceMap пуст без сервисов', () => {
    const map: ServiceMap = {};
    expect(Object.keys(map)).toEqual([]);
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает (модуль не существует)**

Run: `bun test src/core/service.test.ts`
Expected: FAIL, «cannot find module '../index.ts'» / «Cannot find module» (файла `service.ts` ещё нет, экспорт не найден).

- [ ] **Step 3: Реализовать `src/core/service.ts`**

```ts
import type { z } from 'zod';
import type { ChannelMemory, Logger } from './types.ts';

/** Регистр типов сервисов; расширяется сервисами через `declare module` в их service.ts. */
export interface ServiceMap {}

export interface ServiceInitContext<O = unknown> {
  options: O;
  logger: Logger;
  memory: ChannelMemory;
}

export interface ServiceDef<O = undefined> {
  name: string;
  description?: string;
  optionsSchema?: z.ZodType;
  init(ctx: ServiceInitContext<O>): unknown | Promise<unknown>;
  close?(service: unknown): void | Promise<void>;
}

export interface Service<O = undefined> {
  readonly name: string;
  readonly description?: string;
  readonly optionsSchema?: z.ZodType;
  readonly init: (ctx: ServiceInitContext<O>) => unknown | Promise<unknown>;
  readonly close?: (service: unknown) => void | Promise<void>;
}

export function defineService<O = undefined>(def: ServiceDef<O>): Service<O> {
  return {
    name: def.name,
    ...(def.description !== undefined ? { description: def.description } : {}),
    ...(def.optionsSchema !== undefined ? { optionsSchema: def.optionsSchema } : {}),
    init: def.init,
    ...(def.close !== undefined ? { close: def.close } : {}),
  };
}
```

- [ ] **Step 4: Экспортировать из фасада `src/core/index.ts`**

Добавить в `src/core/index.ts`:

```ts
export { defineService } from './service.ts';
export type { Service, ServiceDef, ServiceInitContext, ServiceMap } from './service.ts';
```

- [ ] **Step 5: Прогнать тест и typecheck**

Run: `bun test src/core/service.test.ts && bun run typecheck`
Expected: PASS; `tsc --noEmit` без ошибок.

- [ ] **Step 6: Commit**

```bash
git add src/core/service.ts src/core/index.ts src/core/service.test.ts
git commit -m "feat(services): defineService + ServiceMap contract"
```

---

### Task 2: Контексты модуля и handler-а получают `services: ServiceMap`

**Files:**
- Modify: `src/core/handler.ts`
- Modify: `src/core/module.ts`
- Test: существующие `src/modules/*/module.test.ts` должны остаться зелёными; проверка `bun run typecheck`.

**Interfaces:**
- Consumes: `ServiceMap` из Task 1.
- Produces: `HandlerRunContext.services: ServiceMap`; `ModuleSetupContext.services: ServiceMap`; `ModuleReadyContext` (наследует); `ModuleDef.services?: readonly (keyof ServiceMap)[]`; `Module.services: readonly string[]`.

- [ ] **Step 1: Добавить `services` в `src/core/handler.ts`**

```ts
// вверху файла:
import type { ServiceMap } from './service.ts';

// HandlerRunContext:
export interface HandlerRunContext {
  input: import('./types.ts').Input;
  store: import('./types.ts').Store;
  memory: import('./types.ts').ChannelMemory;
  logger: import('./types.ts').Logger;
  services: ServiceMap;
}
```

- [ ] **Step 2: Добавить `services` в `src/core/module.ts`**

```ts
// вверху файла:
import type { ServiceMap } from './service.ts';

// ModuleSetupContext:
export interface ModuleSetupContext {
  store: Store;
  memory: ChannelMemory;
  logger: Logger;
  commands: CommandCatalog;
  services: ServiceMap;
}

// ModuleDef: добавить поле services (до handlers)
services?: readonly (keyof ServiceMap)[];

// Module: добавить поле
readonly services: readonly string[];

// defineModule: в возвращаемом объекте добавить
services: (def.services ?? []) as readonly string[],
```

- [ ] **Step 3: Убедиться, что существующее зелёное**

Run: `bun test && bun run typecheck`
Expected: 72 pass / 0 fail; `tsc --noEmit` без ошибок (существующие модули без `services` получают пустой `[]` и `ctx.services: {}`).

- [ ] **Step 4: Commit**

```bash
git add src/core/handler.ts src/core/module.ts
git commit -m "feat(services): services: ServiceMap in module and handler contexts"
```

---

### Task 3: `ServiceRegistry` (internal)

**Files:**
- Create: `src/core/internal/service-registry.ts`
- Test: `src/core/internal/service-registry.test.ts`

**Interfaces:**
- Consumes: `Service` из Task 1.
- Produces: `class ServiceRegistry { discover(servicesDir): Promise<void>; find(name): Service | undefined; getServices(): Service[]; size: number; register(svc): void }`.

- [ ] **Step 1: Написать падающий тест**

```ts
import { afterAll, beforeAll, describe, expect, test } from 'bun:test';
import { mkdirSync, rmSync, writeFileSync } from 'node:fs';
import { join } from 'node:path';
import { ServiceRegistry } from './service-registry.ts';

const dir = join(import.meta.dir, '..', '..', '..', '.data', 'service-registry-test');

const hello = `import { defineService } from '../../../../src/core/index.ts';
export default defineService({ name: 'hello', init: () => 'hi' });
`;

const dup1 = `import { defineService } from '../../../../src/core/index.ts';
export default defineService({ name: 'same', init: () => 1 });
`;
const dup2 = dup1.replace("init: () => 1", "init: () => 2");

describe('ServiceRegistry', () => {
  beforeAll(() => {
    rmSync(dir, { recursive: true, force: true });
    mkdirSync(join(dir, 'hello'), { recursive: true });
    mkdirSync(join(dir, 'same'), { recursive: true });
    writeFileSync(join(dir, 'hello', 'service.ts'), hello);
    writeFileSync(join(dir, 'same', 'service.ts'), dup1);
  });

  afterAll(() => rmSync(dir, { recursive: true, force: true }));

  test('discover находит сервисы по конвенции', async () => {
    const reg = new ServiceRegistry();
    await reg.discover(dir);
    expect(reg.size).toBe(2);
    expect(reg.find('hello')).toBeDefined();
    expect(reg.find('missing')).toBeUndefined();
  });

  test('discover не падает на несуществующей папке', async () => {
    const reg = new ServiceRegistry();
    await reg.discover(join(dir, 'no-such-dir'));
    expect(reg.size).toBe(0);
  });

  test('register с дублем имени бросает', () => {
    const reg = new ServiceRegistry();
    reg.register({ name: 'x', init: () => 1 });
    expect(() => reg.register({ name: 'x', init: () => 2 })).toThrow('Дубликат имени сервиса');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `bun test src/core/internal/service-registry.test.ts`
Expected: FAIL — cannot find module `./service-registry.ts`.

- [ ] **Step 3: Реализовать `src/core/internal/service-registry.ts`**

```ts
import { existsSync, readdirSync } from 'node:fs';
import { join } from 'node:path';
import { pathToFileURL } from 'node:url';
import type { Service } from '../service.ts';

/** Реестр сервисов: авто-дискавери по конвенции src/services/<name>/service.ts. */
export class ServiceRegistry {
  private readonly services = new Map<string, Service>();
  private version = 0;

  get size(): number {
    return this.services.size;
  }

  getServices(): Service[] {
    return [...this.services.values()];
  }

  find(name: string): Service | undefined {
    return this.services.get(name);
  }

  async discover(servicesDir: string): Promise<void> {
    this.services.clear();
    this.version += 1;
    if (!existsSync(servicesDir)) return;

    const entries = readdirSync(servicesDir, { withFileTypes: true });
    for (const entry of entries) {
      if (!entry.isDirectory()) continue;
      const file = join(servicesDir, entry.name, 'service.ts');
      if (!existsSync(file)) continue;

      const url = `${pathToFileURL(file).href}?v=${this.version}`;
      const loaded = (await import(url)) as { default?: Service };
      const svc = loaded.default;
      if (!svc || typeof svc.name !== 'string') {
        throw new Error(`Сервис ${entry.name}: service.ts должен экспортировать defineService по умолчанию`);
      }
      this.register(svc);
    }
  }

  /** Регистрирует сервис вручную (discover зовёт это же; публично — для тестов/хоста). */
  register(svc: Service): void {
    if (this.services.has(svc.name)) {
      throw new Error(`Дубликат имени сервиса: ${svc.name}`);
    }
    this.services.set(svc.name, svc);
  }
}
```

- [ ] **Step 4: Прогнать тест**

Run: `bun test src/core/internal/service-registry.test.ts`
Expected: PASS (3 теста).

- [ ] **Step 5: Commit**

```bash
git add src/core/internal/service-registry.ts src/core/internal/service-registry.test.ts
git commit -m "feat(services): ServiceRegistry with auto-discovery"
```

---

### Task 4: Секция `services` в `BotConfig`

**Files:**
- Modify: `src/core/internal/config.ts`
- Test: `src/core/internal/config.test.ts`

**Interfaces:**
- Produces: `BotConfig.services?: Record<string, ModuleEntry>` (переиспользует существующий `ModuleEntry`).

- [ ] **Step 1: Добавить падающий кейс в `src/core/internal/config.test.ts`**

```ts
test('читает секцию services', async () => {
  const dir2 = join(import.meta.dir, '..', '..', '..', '.data', 'config-test');
  const withServices = join(dir2, 'services.config.ts');
  writeFileSync(
    withServices,
    'export default { modules: {}, services: { weather: { options: { apiKey: "k" } } } };',
  );
  const config = await loadConfig(withServices);
  expect(config.services?.weather).toEqual({ options: { apiKey: 'k' } });
});
```

(добавить перед `afterAll`; `writeFileSync` уже импортирован в файле)

- [ ] **Step 2: Убедиться, что падает**

Run: `bun test src/core/internal/config.test.ts`
Expected: FAIL — `config.services` отсутствует (тип не объявлен → `config.services?.weather` ошибка TS/рантайм undefined).

- [ ] **Step 3: Реализовать**

В `src/core/internal/config.ts`, в `BotConfig` после `modules`:

```ts
/** Опции и включение сервисов (services.<name>.options). */
services?: Record<string, ModuleEntry>;
```

- [ ] **Step 4: Прогнать тест и typecheck**

Run: `bun test src/core/internal/config.test.ts && bun run typecheck`
Expected: PASS; tsc чисто.

- [ ] **Step 5: Commit**

```bash
git add src/core/internal/config.ts src/core/internal/config.test.ts
git commit -m "feat(services): BotConfig.services section"
```

---

### Task 5: Жизненный цикл — init/close, инжекция, compose, interactor

**Files:**
- Modify: `src/app/lifecycle.ts`
- Modify: `src/app/compose.ts`
- Modify: `src/app/interactor.ts`
- Test: `src/app/lifecycle.test.ts` (новый)

**Interfaces:**
- Consumes: `ServiceRegistry` (Task 3), `ServiceMap` (Task 1), `BotConfig.services` (Task 4), `Module.services` (Task 2).
- Produces: `ComposeOptions.servicesDir?: string`; `AppContext` (без изменений по полям); `InteractionInteractor` deps получает `servicesFor(moduleName: string): ServiceMap`.

- [ ] **Step 1: Написать падающий тест `src/app/lifecycle.test.ts`**

Тест строит app через `composeApp` в offline-режиме с временными модулями и сервисами.

```ts
import { afterAll, beforeAll, describe, expect, test } from 'bun:test';
import { mkdirSync, rmSync, writeFileSync } from 'node:fs';
import { join } from 'node:path';
import { composeApp } from './compose.ts';

const dir = join(import.meta.dir, '..', '..', '.data', 'lifecycle-services-test');
const modulesDir = join(dir, 'modules');
const servicesDir = join(dir, 'services');

const logger = { debug: () => {}, info: () => {}, warn: () => {}, error: () => {} };

beforeAll(() => {
  rmSync(dir, { recursive: true, force: true });
  mkdirSync(join(servicesDir, 'weather'), { recursive: true });
  mkdirSync(join(modulesDir, 'forecast'), { recursive: true });

  writeFileSync(
    join(servicesDir, 'weather', 'service.ts'),
    `import { defineService } from '../../../../src/core/index.ts';
export default defineService({
  name: 'weather',
  init: () => ({ now: () => '+10' }),
  close: () => { globalThis.__weatherClosed = true; },
});
`,
  );

  writeFileSync(
    join(modulesDir, 'forecast', 'module.ts'),
    `import { defineHandler, defineModule } from '../../../../src/core/index.ts';
export default defineModule({
  name: 'forecast',
  services: ['weather'],
  handlers: [
    defineHandler({
      name: 'forecast',
      description: 'Погода',
      run: ({ services }) => ({ kind: 'message', content: services.weather.now() }),
    }),
  ],
});
`,
  );
});

afterAll(() => {
  delete (globalThis as Record<string, unknown>).__weatherClosed;
  rmSync(dir, { recursive: true, force: true });
});

describe('composeApp с сервисами (offline)', () => {
  test('init сервиса происходит до setup и run команды его видит', async () => {
    const app = composeApp({ modules: { forecast: { enabled: true } } }, {
      modulesDir,
      servicesDir,
      syncSlashCommands: false,
      logger,
    });
    await app.lifecycle.start();
    const result = await app.interactor.handle(
      { commandName: 'forecast', args: {}, author: { id: 'u1', username: 'U' }, channel: { id: 'c1', guildId: 'g1' } },
      { preconditions: {}, granted: new Set() },
    );
    expect(result).toMatchObject({ kind: 'message' });
    if (result?.kind === 'message') expect(result.content).toBe('+10');
    await app.lifecycle.shutdown();
  });

  test('модуль с неизвестным сервисом — ошибка старта', async () => {
    const app = composeApp({ modules: { forecast: { enabled: true } } }, {
      modulesDir,
      servicesDir: join(dir, 'empty-services'),
      syncSlashCommands: false,
      logger,
    });
    await expect(app.lifecycle.start()).rejects.toThrow('weather');
  });

  test('close сервиса вызывается на shutdown', async () => {
    const app = composeApp({ modules: { forecast: { enabled: true } } }, {
      modulesDir,
      servicesDir,
      syncSlashCommands: false,
      logger,
    });
    await app.lifecycle.start();
    await app.lifecycle.shutdown();
    expect((globalThis as Record<string, unknown>).__weatherClosed).toBe(true);
  });
});
```

- [ ] **Step 2: Убедиться, что падает**

Run: `bun test src/app/lifecycle.test.ts`
Expected: FAIL — `composeApp` не принимает `servicesDir`, сервисы не строятся.

- [ ] **Step 3: Реализовать `src/app/interactor.ts`**

```ts
import type { ServiceMap } from '../core/service.ts';

// в InteractorDeps добавить:
servicesFor(moduleName: string): ServiceMap;

// в handle(): заменить строку построения ctx:
const ctx = {
  input,
  store,
  memory: this.deps.memory,
  logger: this.deps.logger,
  services: this.deps.servicesFor(module.name),
};
```

- [ ] **Step 4: Реализовать `src/app/lifecycle.ts`**

Добавить в `LifecycleDeps`:

```ts
serviceRegistry: ServiceRegistry;
servicesDir: string;
services: Map<string, unknown>;
```

В `start()`, после `await registry.discover(...)`:

```ts
await this.deps.serviceRegistry.discover(this.deps.servicesDir);
```

После `this.resolveEnabledModules()` и лога «Обнаружено модулей» — вызвать:

```ts
await this.initServices();
```

В `runSetup()` заменить построение ctx:

```ts
const ctx = {
  store,
  memory: this.deps.memory,
  logger: this.deps.logger,
  commands: this.commands,
  services: this.servicesMap(),
};
```

В `runModuleReady()` добавить в ctx:

```ts
services: this.servicesMap(),
```

В `shutdown()`, после цикла `onShutdown` модулей, добавить:

```ts
for (const [name, api] of [...this.deps.services].reverse()) {
  const svc = this.deps.serviceRegistry.find(name);
  if (!svc?.close) continue;
  try {
    await svc.close(api);
  } catch (err) {
    this.deps.logger.error(`close сервиса "${name}" упал`, err);
  }
}
```

Добавить приватные методы:

```ts
private servicesMap(): ServiceMap {
  return Object.fromEntries(this.deps.services) as ServiceMap;
}

private async initServices(): Promise<void> {
  const needed = new Set<string>();
  for (const mod of this.enabledModules) {
    for (const name of mod.services ?? []) needed.add(name);
  }
  for (const name of needed) {
    const svc = this.deps.serviceRegistry.find(name);
    if (!svc) throw new Error(`Сервис "${name}" объявлен модулями, но не найден в src/services`);
    const entry = this.deps.config.services?.[name];
    if (entry && entry.enabled === false) {
      throw new Error(`Сервис "${name}" отключён в конфиге, но нужен модулю`);
    }
    const options = this.serviceOptions(svc, entry);
    try {
      const api = await svc.init({ options, logger: this.deps.logger, memory: this.deps.memory });
      this.deps.services.set(name, api);
    } catch (err) {
      throw new Error(`Сервис "${name}": init упал: ${String(err)}`);
    }
  }
}

private serviceOptions(svc: Service, entry: ModuleEntry | undefined): unknown {
  const schema = svc.optionsSchema as z.ZodType | undefined;
  const raw = entry?.options ?? {};
  if (!schema) return raw;
  const result = schema.safeParse(raw);
  if (!result.success) {
    const issues = result.error.issues.map((i) => `${i.path.join('.')}: ${i.message}`).join('; ');
    throw new Error(`Опции сервиса "${svc.name}" невалидны: ${issues}`);
  }
  return result.data;
}
```

Импорты: `import type { Service } from '../core/service.ts';` и `import type { ServiceRegistry } from '../core/internal/service-registry.ts';` (`z` уже импортирован).

- [ ] **Step 5: Реализовать `src/app/compose.ts`**

```ts
// импорт ServiceRegistry
import { ServiceRegistry } from '../core/internal/service-registry.ts';
import type { ServiceMap } from '../core/service.ts';

// ComposeOptions: добавить
servicesDir?: string;

// в composeApp() после const stores = new Map...:
const services = new Map<string, unknown>();
const serviceRegistry = new ServiceRegistry();

// interactor deps: добавить
servicesFor: () => Object.fromEntries(services) as ServiceMap,

// lifecycle deps: добавить
serviceRegistry,
servicesDir: options.servicesDir ?? 'src/services',
services,
```

- [ ] **Step 6: Прогнать новый тест + весь набор**

Run: `bun test src/app/lifecycle.test.ts && bun test && bun run typecheck && bun run check:boundaries`
Expected: lifecycle.test.ts PASS (3 теста); весь набор зелёный; tsc чисто; границы ок.

- [ ] **Step 7: Commit**

```bash
git add src/app/lifecycle.ts src/app/compose.ts src/app/interactor.ts src/app/lifecycle.test.ts
git commit -m "feat(services): lifecycle init/close and injection into contexts"
```

---

### Task 6: Тест-хелперы принимают `services`

**Files:**
- Modify: `src/core/testing.ts`
- Test: `src/core/testing.test.ts` (новый) + проверка существующих

**Interfaces:**
- Produces: `TestContext.services: ServiceMap`; `createContext` по умолчанию `services: {}`; `runHandler(handler, { services? })` прокладывает в ctx.

- [ ] **Step 1: Написать падающий тест**

```ts
import { describe, expect, test } from 'bun:test';
import { createContext, runHandler } from './testing.ts';
import { defineHandler } from './index.ts';

describe('testing helpers и services', () => {
  test('createContext даёт пустой services', () => {
    expect(createContext().services).toEqual({});
  });

  test('runHandler пробрасывает services в run', async () => {
    const handler = defineHandler({
      name: 't',
      description: 't',
      run: ({ services }) => {
        const api = services as { echo?: { get(): string } };
        return { kind: 'message', content: api.echo?.get() ?? 'нет' };
      },
    });
    const result = await runHandler(handler, {
      services: { echo: { get: () => 'есть' } },
    });
    if (result.kind === 'message') expect(result.content).toBe('есть');
  });
});
```

- [ ] **Step 2: Убедиться, что падает**

Run: `bun test src/core/testing.test.ts`
Expected: FAIL — `createContext().services` undefined.

- [ ] **Step 3: Реализовать в `src/core/testing.ts`**

```ts
// импорт
import type { ServiceMap } from './service.ts';

// TestContext: добавить поле
services: ServiceMap;

// createContext: в объект добавить
services: {} as ServiceMap,

// runHandler: options-тип расширить { input?; args?; services? } и применить:
export async function runHandler(
  handler: Handler,
  options: { input?: Input; args?: Record<string, unknown>; services?: ServiceMap } = {},
): Promise<Result> {
  const base = createContext();
  const input = options.input ?? { ...base.input, args: options.args ?? {} };
  const services = options.services ?? base.services;
  return new Pipeline().run(handler, { ...base, input, services });
}
```

- [ ] **Step 4: Прогнать тесты и typecheck**

Run: `bun test src/core/testing.test.ts && bun test && bun run typecheck`
Expected: PASS; весь набор зелёный; tsc чисто.

- [ ] **Step 5: Commit**

```bash
git add src/core/testing.ts src/core/testing.test.ts
git commit -m "feat(services): testing helpers accept services"
```

---

### Task 7: Границы — сканирование `src/services`

**Files:**
- Modify: `scripts/check-boundaries.ts`

- [ ] **Step 1: Расширить скрипт**

Заменить блок сканирования (от `const failures: string[] = [];` до конца) на:

```ts
const failures: string[] = [];
const ROOTS = [MODULES_DIR, SERVICES_DIR];

for (const root of ROOTS) {
  for (const file of walk(root)) {
    for (const spec of importsOf(file)) {
      if (spec.includes('discord.js')) {
        failures.push(`${file}: запрещён прямой импорт discord.js («${spec}»); используйте только core/index.ts`);
      }
      if (spec.startsWith('../../core/') && ![...ALLOWED_CORE].some((a) => spec === a)) {
        failures.push(
          `${file}: импорт вне публичного контракта («${spec}»); разрешены только core/index.ts и core/testing.ts`,
        );
      }
    }
  }
}

if (failures.length > 0) {
  console.error('Нарушение границ:');
  for (const f of failures) console.error(`  - ${f}`);
  process.exit(1);
}

const total = walk(MODULES_DIR).length + walk(SERVICES_DIR).length;
console.log(`Границы ок: ${total} файлов модулей и сервисов, все импорты в рамках контракта.`);
```

и в начале файла после `const MODULES_DIR`:

```ts
const SERVICES_DIR = 'src/services';
```

- [ ] **Step 2: Проверить**

Run: `bun run check:boundaries`
Expected: «Границы ок: N файлов модулей и сервисов, все импорты в рамках контракта.» (src/services пока отсутствует или пуст — walk возвращает []; N равен числу файлов модулей).

- [ ] **Step 3: Commit**

```bash
git add scripts/check-boundaries.ts
git commit -m "chore(services): boundary check covers src/services"
```

---

### Task 8: Демо — сервис weather + модуль forecast

**Files:**
- Create: `src/services/weather/service.ts`
- Create: `src/modules/forecast/module.ts`
- Test: `src/modules/forecast/module.test.ts`
- Modify: `bot.config.ts`

**Interfaces:**
- Consumes: `defineService` (Task 1), `defineModule`/`defineHandler`/`arg` (существующие), `ServiceMap` (Task 1).
- Produces: демонстрация аугментации `ServiceMap` и `services: ['weather']`.

- [ ] **Step 1: Создать `src/services/weather/service.ts`**

```ts
import { z } from 'zod';
import { defineService } from '../../core/index.ts';

export interface WeatherApi {
  now(city: string): Promise<string>;
}

declare module '../../core/index.ts' {
  interface ServiceMap {
    weather: WeatherApi;
  }
}

export default defineService({
  name: 'weather',
  description: 'Демо-сервис погоды (эмуляция внешнего API)',
  optionsSchema: z.object({ apiKey: z.string().optional() }),
  init: ({ options, logger }) => {
    logger.info(`weather: init (apiKey=${options.apiKey ? 'задан' : 'не задан'})`);
    return {
      async now(city) {
        const base = options.apiKey?.length ?? 3;
        return `В городе ${city} сейчас +${base * 3}°C (демо)`;
      },
    } satisfies WeatherApi;
  },
});
```

- [ ] **Step 2: Создать `src/modules/forecast/module.ts`**

```ts
import { arg, defineHandler, defineModule } from '../../core/index.ts';

export default defineModule({
  name: 'forecast',
  description: 'Погода через сервис weather',
  services: ['weather'],
  handlers: [
    defineHandler({
      name: 'forecast',
      description: 'Погода в городе',
      args: {
        city: arg.string('Город').default('Москва'),
      },
      run: async ({ services, args }) => {
        const text = await services.weather.now(args.city);
        return { kind: 'message', content: text };
      },
    }),
  ],
});
```

- [ ] **Step 3: Написать тест `src/modules/forecast/module.test.ts`**

```ts
import { describe, expect, test } from 'bun:test';
import { runHandler } from '../../core/testing.ts';
import module from './module.ts';

describe('модуль forecast', () => {
  test('команда использует сервис weather', async () => {
    const handler = module.handlers.find((h) => h.name === 'forecast')!;
    const result = await runHandler(handler, {
      args: { city: 'Казань' },
      services: { weather: { now: async () => 'Казань: +10°' } },
    });
    expect(result).toMatchObject({ kind: 'message' });
    if (result.kind === 'message') expect(result.content).toContain('Казань');
  });

  test('дефолтный город — Москва', async () => {
    const handler = module.handlers.find((h) => h.name === 'forecast')!;
    const result = await runHandler(handler, {
      services: { weather: { now: async (city) => `${city}` } },
    });
    if (result.kind === 'message') expect(result.content).toBe('Москва');
  });
});
```

- [ ] **Step 4: Включить модуль в `bot.config.ts`**

Добавить `forecast: { enabled: true },` в секцию `modules` (после roll).

- [ ] **Step 5: Прогнать**

Run: `bun test src/modules/forecast/module.test.ts && bun test && bun run typecheck && bun run check:boundaries`
Expected: PASS (2 новых теста); весь набор зелёный; tsc чисто (проверяет аугментацию `ServiceMap` — `services.weather` типизирован как `WeatherApi`); границы ок (weather/service.ts и forecast/module.ts в сканировании).

- [ ] **Step 6: Commit**

```bash
git add src/services/weather/service.ts src/modules/forecast/module.ts src/modules/forecast/module.test.ts bot.config.ts
git commit -m "feat(services): demo weather service + forecast module"
```

---

### Task 9: Генератор `create:service`

**Files:**
- Create: `scripts/create-service.ts`
- Modify: `package.json`
- Modify: `scripts/create-module.ts` (строка-подсказка про сервисы)

- [ ] **Step 1: Создать `scripts/create-service.ts`**

```ts
import { mkdirSync, writeFileSync } from 'node:fs';
import { join } from 'node:path';

const name = process.argv[2];
if (!name || !/^[a-z0-9-]+$/.test(name)) {
  console.error('Использование: bun run create:service <name> (только a-z, 0-9, -)');
  process.exit(1);
}

const dir = join('src', 'services', name);
mkdirSync(dir, { recursive: true });

const template = `import { defineService } from '../../core/index.ts';

export interface ${name}Api {
  // TODO: методы API сервиса
}

declare module '../../core/index.ts' {
  interface ServiceMap {
    ${name}: ${name}Api;
  }
}

export default defineService({
  name: '${name}',
  description: 'Что делает сервис',
  init: () => {
    // TODO: инициализация (клиент, соединение)
    return {} satisfies ${name}Api;
  },
  // close: (api) => { /* teardown */ },
});
`;

writeFileSync(join(dir, 'service.ts'), template);

console.log(`Создан сервис: src/services/${name}/`);
console.log('Дальше:');
console.log('  1. Отредактируйте src/services/<name>/service.ts');
console.log('  2. В модуле укажите services: [\'<name>\'] и используйте ctx.services.<name>');
console.log('  3. Опции сервиса — в bot.config.ts: services.<name>.options (схема optionsSchema)');
```

- [ ] **Step 2: Добавить script в `package.json`**

В секцию `scripts` после `create:module`:

```json
"create:service": "bun scripts/create-service.ts",
```

- [ ] **Step 3: Подсказка в `scripts/create-module.ts`**

В `moduleTemplate` после строки `description: 'Что делает этот модуль',` добавить:

```ts
  // services: ['my-service'],  // сервис из src/services/, доступен в ctx.services
```

- [ ] **Step 4: Проверить**

Run: `bun run create:service echo && ls src/services/echo/service.ts && rm -rf src/services/echo && bun run typecheck`
Expected: файл создан с шаблоном; затем удалён; tsc чисто.

- [ ] **Step 5: Commit**

```bash
git add scripts/create-service.ts scripts/create-module.ts package.json
git commit -m "feat(services): create:service generator"
```

---

### Task 10: Документация

**Files:**
- Create: `docs/adr/0008-services-as-lifecycle-managed-dependencies.md`
- Modify: `CONTEXT.md`, `docs/guides/module-api.md`, `docs/llm.md`, `AGENTS.md`, `CLAUDE.md`, `README.md`
- Verify: `bun run docs:build`

- [ ] **Step 1: ADR-0008**

```markdown
# Services as lifecycle-managed dependencies

Сервис — именованная глобальная зависимость модулей (API-клиент, соединение и т.п.), живущая в `src/services/<name>/service.ts` и объявленная через `defineService`. Сервисы расширяют интерфейс `ServiceMap` (аугментация типов), поэтому `ctx.services.<name>` типизирован. Модуль декларирует нужные сервисы (`services: ['name']`) — ядро строит только их (init до setup модулей, опции из `bot.config.ts` секции `services` с zod-валидацией) и закрывает на shutdown в обратном порядке. Внешние API/БД — ответственность сервиса, а не модуля: это перенос решения ADR-0002 (framework-owned state) на уровень зависимостей. Типизация `ctx.services` в handler-ах — через полный `ServiceMap` (fallback из спеки): декларация `services` остаётся гейтом сборки и валидации.
```

- [ ] **Step 2: Глоссарий `CONTEXT.md`**

Добавить термин:

- **Service** — именованная глобальная зависимость модуля (API-клиент, соединение), живёт в `src/services/<name>/service.ts`, объявлена через `defineService`, расширяет `ServiceMap`; модуль получает её типизированно в `ctx.services`. (Avoid: plugin, connector, third-party)

- [ ] **Step 3: `docs/guides/module-api.md` — секция про сервисы**

Добавить раздел «Сервисы» (после раздела про Store): как объявить сервис, как подключить к модулю (`services: ['name']`), `ctx.services.<name>`, опции в `bot.config.ts`, пример из `src/services/weather` + `src/modules/forecast`.

- [ ] **Step 4: `docs/llm.md` — секция про сервисы**

Добавить пункт 6.1 «Сервисы (внешние зависимости)»: где живут, как декларируются, жизненный цикл init/close, опции. Обновить дерево структуры (добавить `src/services/`) и таблицу видимости (сервисы импортируют только core/index.ts). Обновить глоссарий (Service). Обновить do/don't (не создавать API-клиенты внутри модуля — это сервис).

- [ ] **Step 5: `AGENTS.md` / `CLAUDE.md`**

В разделы Hard rules добавить строку:

```text
- Services live in `src/services/<name>/service.ts` (defineService); modules declare them via `services: ['name']` and use typed `ctx.services.<name>`. Services may import only the core facade, like modules.
```

- [ ] **Step 6: `README.md`**

В дерево структуры после `└── modules/` добавить строку `├── services/           # сервисы: weather, ... (defineService)` и упомянуть в блоке команд `bun run create:service`.

- [ ] **Step 7: Проверить сборку доков**

Run: `bun run docs:build`
Expected: build complete без ошибок.

- [ ] **Step 8: Финальный прогон**

Run: `bun test && bun run typecheck && bun run check:boundaries && bun run test:coverage`
Expected: всё зелёное; покрытие ≥ 0.7.

- [ ] **Step 9: Commit**

```bash
git add docs/adr/0008-services-as-lifecycle-managed-dependencies.md CONTEXT.md docs/guides/module-api.md docs/llm.md AGENTS.md CLAUDE.md README.md
git commit -m "docs(services): ADR-0008, glossary, guides, agent docs"
```

---

## Self-Review

**Spec coverage:**
- defineService + ServiceMap → Task 1
- аугментация типов + fallback → Task 2, Task 8 (typecheck доказывает)
- ServiceRegistry → Task 3
- config.services + zod-валидация → Task 4, Task 5 (serviceOptions)
- init до setup, close обратный порядок, build только нужных → Task 5
- неизвестный сервис → ошибка старта → Task 5 (тест)
- ctx.services в setup/onReady/run → Task 2, Task 5
- тест-хелперы → Task 6
- границы src/services → Task 7
- create:service → Task 9
- ADR-0008, глоссарий, гайды, агент-доки → Task 10
- демо weather/forecast → Task 8
- вне скоупа (service→service, hot reload, per-handler) — не в плане, намеренно.

**Placeholder scan:** в create-service.ts шаблон содержит комментарии `// TODO: ...` — это содержимое генерируемого шаблона для пользователя (намеренно), не заглушка плана.

**Type consistency:** `servicesFor(moduleName): ServiceMap` в interactor и compose согласованы; `ServiceRegistry.discover/find/register` совпадают во всех тасках; `defineService` возвращает `Service`, registry хранит `Service`. Проверено по сигнатурам между тасками.
